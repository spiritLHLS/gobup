# GoBup 环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# ===== 认证配置 =====
# 管理员用户名（推荐设置，仅首次启动时创建管理员账户）
GOBUP_USERNAME=admin

# 管理员密码（推荐设置，仅首次启动时创建管理员账户）
# 注意：请使用强密码，建议包含大小写字母、数字和特殊字符
GOBUP_PASSWORD=your_secure_password_here

# ===== Docker 部署配置 =====
# 对于 Docker 部署，使用以下变量名
# USERNAME=admin
# PASSWORD=your_secure_password_here

# ===== 其他配置 =====
# 时区设置（可选，默认 Asia/Shanghai）
TZ=Asia/Shanghai

# ===== 安装版本 =====
# 指定安装的版本（可选，默认为最新版本）
# INSTALL_VERSION=v20250101-120000

# ===== 使用说明 =====
# 1. 安装脚本部署：
#    GOBUP_USERNAME=admin GOBUP_PASSWORD=123456 bash install.sh
#
# 2. Docker 部署：
#    docker run -d \
#      -e USERNAME=admin \
#      -e PASSWORD=123456 \
#      -p 22380:12380 \
#      -v ./recordings:/rec \
#      -v ./data:/app/data \
#      spiritlhl/gobup:latest
#
# 3. Docker Compose 部署：
#    在 docker-compose.yml 中设置环境变量或使用 .env 文件
#
# 4. 手动运行：
#    ./gobup-server -username admin -password 123456 -port 12380 -work-path /path/to/recordings

# ===== 安全提示 =====
# - 请勿将包含真实密码的 .env 文件提交到版本控制系统
# - 建议使用强密码（至少12位，包含大小写字母、数字和特殊字符）
# - 定期备份数据库文件 ./data/gobup.db
# - 如需修改密码，需删除数据库重新初始化
